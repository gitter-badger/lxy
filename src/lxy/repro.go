package main 

// Utilities for reproducing analyses. Takes a 
/*
type Analysis []Command

type Command struct {
	cmd string
	files 
}

// Reproduce analysis reproduces an analysis performed using LXY as specified in an input YAML file.
func ReproduceAnalysis(path string) error {

	// Read the YAML, if possible
	y, e := ReadYAML(path)
	if e != nil {
		return e
	}

	// The following will loop through each of the steps of the analysis in
	// order and run the step, provided the necessary input file is available.
	for _, step := range Analysis {

		// Check that the necessary input data is available



	}

}

// ReadYAML reads a YAML secification of an analysis to perform into an array
//
// ReadYAML will fail if a YAML is being read that is either malformatted or 
// does not exist.
//
// If successful, ReadYAML will return an array of maps describing a series of
// analytical steps to perform.
func ReadYAML(path string) (Analysis, error) {

	// If the YAML file does not exist or is malformatted, return that error

	return nil
}
*/

